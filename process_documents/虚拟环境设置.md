# Python 虚拟环境设置指南

## 为什么使用虚拟环境？

- ✅ 隔离项目依赖，避免版本冲突
- ✅ 保持系统Python环境干净
- ✅ 便于项目迁移和部署
- ✅ 不同项目可以使用不同版本的库

---

## 方法一：使用 venv（推荐，Python 3.3+自带）

### 1. 创建虚拟环境

```bash
# 进入项目目录
cd Desktop/kaoLong

# 创建虚拟环境（名为 venv）
python3 -m venv venv
```

### 2. 激活虚拟环境

**macOS/Linux:**
```bash
source venv/bin/activate
```

**Windows:**
```bash
# CMD
venv\Scripts\activate.bat

# PowerShell
venv\Scripts\Activate.ps1
```

激活后，命令行前面会显示 `(venv)`，表示已进入虚拟环境。

### 3. 安装项目依赖

```bash
# 确保在虚拟环境中（命令行前有 (venv) 标识）
pip install -r requirements.txt
```

### 4. 验证安装

```bash
# 查看已安装的包
pip list

# 测试系统
python test_system.py
```

### 5. 退出虚拟环境

```bash
deactivate
```

---

## 方法二：使用 conda（如果你安装了 Anaconda）

### 1. 创建虚拟环境

```bash
# 创建名为 yijian 的环境，使用 Python 3.10
conda create -n yijian python=3.10

# 或者使用 Python 3.11
conda create -n yijian python=3.11
```

### 2. 激活虚拟环境

```bash
conda activate yijian
```

### 3. 安装依赖

```bash
# 进入项目目录
cd Desktop/kaoLong

# 安装依赖
pip install -r requirements.txt
```

### 4. 退出虚拟环境

```bash
conda deactivate
```

---

## 完整工作流程示例

### 首次设置（只需执行一次）

```bash
# 1. 进入项目目录
cd Desktop/kaoLong

# 2. 创建虚拟环境
python3 -m venv venv

# 3. 激活虚拟环境
source venv/bin/activate  # macOS/Linux
# 或
venv\Scripts\activate  # Windows

# 4. 升级 pip（可选但推荐）
pip install --upgrade pip

# 5. 安装项目依赖
pip install -r requirements.txt

# 6. 配置环境变量
cp .env .env
# 然后编辑 .env 文件，设置 OPENAI_API_KEY

# 7. 测试系统
python test_system.py

# 8. 初始化知识库
python init_knowledge_base.py
```

### 日常使用

```bash
# 1. 进入项目目录
cd Desktop/kaoLong

# 2. 激活虚拟环境
source venv/bin/activate  # macOS/Linux
# 或
venv\Scripts\activate  # Windows

# 3. 使用AI助手
python main.py

# 4. 完成后退出虚拟环境
deactivate
```

---

## 常见问题

### Q1: 如何确认虚拟环境已激活？

**A:** 激活后，命令行提示符前会显示 `(venv)`，例如：
```
(venv) user@computer:~/Desktop/kaoLong$
```

### Q2: 虚拟环境在哪里？

**A:** 在项目目录下的 `venv` 文件夹中。这个文件夹已添加到 `.gitignore`，不会被提交到版本控制。

### Q3: 如何删除虚拟环境？

**A:** 直接删除 `venv` 文件夹即可：
```bash
# 确保先退出虚拟环境
deactivate

# 删除虚拟环境
rm -rf venv  # macOS/Linux
# 或
rmdir /s venv  # Windows
```

### Q4: 忘记激活虚拟环境就安装了包怎么办？

**A:** 包会被安装到系统Python中。建议：
1. 卸载刚安装的包（如果不想污染系统环境）
2. 激活虚拟环境
3. 重新安装

### Q5: 如何在 VS Code 中使用虚拟环境？

**A:** 
1. 打开项目文件夹
2. 按 `Cmd+Shift+P` (macOS) 或 `Ctrl+Shift+P` (Windows)
3. 输入 "Python: Select Interpreter"
4. 选择 `./venv/bin/python` 或 `.\venv\Scripts\python.exe`

### Q6: 虚拟环境占用多少空间？

**A:** 通常 200-500 MB，取决于安装的包数量。

---

## 快速命令参考

### venv 方式

```bash
# 创建
python3 -m venv venv

# 激活 (macOS/Linux)
source venv/bin/activate

# 激活 (Windows)
venv\Scripts\activate

# 退出
deactivate

# 删除
rm -rf venv
```

### conda 方式

```bash
# 创建
conda create -n yijian python=3.10

# 激活
conda activate yijian

# 退出
conda deactivate

# 删除
conda remove -n yijian --all

# 查看所有环境
conda env list
```

---

## 最佳实践

1. **总是在虚拟环境中工作**
   - 开发前先激活虚拟环境
   - 完成后退出虚拟环境

2. **不要提交虚拟环境到版本控制**
   - `venv` 文件夹已在 `.gitignore` 中

3. **保持 requirements.txt 更新**
   ```bash
   # 导出当前环境的依赖
   pip freeze > requirements.txt
   ```

4. **使用特定版本号**
   - requirements.txt 中指定版本可以避免兼容性问题

5. **定期清理**
   ```bash
   # 查看已安装的包
   pip list
   
   # 卸载不需要的包
   pip uninstall package_name
   ```

---

## 故障排除

### 问题：激活虚拟环境时提示权限错误（Windows PowerShell）

**解决方案：**
```powershell
# 以管理员身份运行 PowerShell，执行：
Set-ExecutionPolicy RemoteSigned

# 然后重新激活虚拟环境
```

### 问题：找不到 python3 命令

**解决方案：**
```bash
# 尝试使用 python 而不是 python3
python -m venv venv

# 或者检查 Python 是否已安装
python --version
python3 --version
```

### 问题：pip 安装速度很慢

**解决方案：**
```bash
# 使用国内镜像源（清华大学）
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# 或者永久配置
pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple
```

---

## 总结

推荐使用 **venv** 方式，因为：
- Python 自带，无需额外安装
- 轻量级，启动快
- 适合大多数项目

祝你使用愉快！🎉

